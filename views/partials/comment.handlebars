

<div class="comment">
    
    {{#each this}}
    <section class="card">
        <div class="card-header">
            <h6 class="mt-2 text-dark cardd subtitle"><i class="fas fa-user mr-2">{{ user.username }}</i></h6>
            </div>>
            <div class="card-body">
                {{comment}}
            </div>
    </section>
    {{/each}}
</div>


<script src="/comment.js"></script>